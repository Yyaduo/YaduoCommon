jdk:
  - openjdk17
install:
  # 跳过认证校验，仅执行编译AAR的命令
  - ./gradlew clean :common:assembleRelease -PskipPublishCheck=true --no-daemon
  # 将编译好的AAR复制到JitPack默认的输出目录，方便JitPack识别
  - mkdir -p build/libs
  - cp common/build/outputs/aar/common-release.aar build/libs/common-${VERSION}.aar